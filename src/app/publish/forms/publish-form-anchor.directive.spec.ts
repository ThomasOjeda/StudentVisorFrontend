import { PublishFormAnchorDirective } from './publish-form-anchor.directive';

describe('PublishFormAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new PublishFormAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
