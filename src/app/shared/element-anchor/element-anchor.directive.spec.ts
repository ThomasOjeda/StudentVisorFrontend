import { ElementAnchorDirective } from './element-anchor.directive';

describe('ElementAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
