/* export const environment = {
  production: true,
  apiUrl: '', //Will default to the current url of the server that served the frontend
}; */
export const environment = {
  production: true,
  apiUrl: 'http://206.189.202.126:5000', //Will default to the current url of the server that served the frontend
};
